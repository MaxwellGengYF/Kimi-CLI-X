from kimi_utils import *
prompt('''
Write a 'Designer' class in manager/designer.py:
* contain a '_folder' for a sub-folder, mkdir in __init__
* contain a 'Worker' class in manager/worker.py
* contain a 'work' function, that take a input file path as argument 'request', use 'create_session()' and  'prompt()' function in kimi_utils.py, to tell Agent to summarize 'request', and return 
''')